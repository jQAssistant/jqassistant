jqassistant:
  scan:
    continue-on-error: ${continueOnError:true}
    reset: true
    include:
      files:
        - java:classpath::~/my-project/build/classes
    properties:
      m2repo.artifacts.scan: true
