<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
<modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.buschmais.jqassistant.sonar</groupId>
        <artifactId>jqassistant.sonar</artifactId>
        <version>1.1.3-SNAPSHOT</version>
    </parent>
    <artifactId>jqassistant.sonar.plugin</artifactId>
    <packaging>sonar-plugin</packaging>

    <description>This plugin provides analysis results to be displayed in SonarQube.</description>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.sonar</groupId>
                <artifactId>sonar-packaging-maven-plugin</artifactId>
                <configuration>
                    <pluginKey>jqassistantbase</pluginKey>
                    <pluginName>jQAssistant</pluginName>
                    <pluginClass>com.buschmais.jqassistant.sonar.plugin.JQAssistantPlugin</pluginClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>native2ascii-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>com.buschmais.jqassistant.core</groupId>
            <artifactId>jqassistant.core.plugin</artifactId>
        </dependency>
        <!-- FIXME: Normally this sonar-plugin needs only the two modules below, but the 'sonarrules' module will parse all concepts/constraints 
             from the ...plugin.all bundle, that's bringing us into trouble with duplicated jar's in the different sonar-plugins in same classpath -->
        <dependency>
            <groupId>com.buschmais.jqassistant.plugin</groupId>
            <artifactId>jqassistant.plugin.all</artifactId>
        </dependency>        
        <!-- dependency>
        	//TODO: Dependencies from 'report' to 'analysis' etc. does bloat the jar to much 
            <groupId>com.buschmais.jqassistant.core</groupId>
            <artifactId>jqassistant.core.report</artifactId>
        </dependency>
        <dependency>
        	//TODO: language support should be deployed as separate plugin (for every language?)
            <groupId>com.buschmais.jqassistant.plugin</groupId>
            <artifactId>jqassistant.plugin.java</artifactId>
            <version>${project.version}</version>
        </dependency-->
        
        <!-- SonarQube -->
        <dependency>
	        <groupId>org.codehaus.sonar</groupId>
            <artifactId>sonar-plugin-api</artifactId>
			<scope>provided</scope>
        </dependency>
        <dependency>
        	<groupId>org.codehaus.sonar-plugins.java</groupId>
        	<artifactId>sonar-java-plugin</artifactId>
        	<type>sonar-plugin</type>
        	<scope>provided</scope>
        </dependency>
		<dependency>
			<groupId>org.codehaus.sonar</groupId>
			<artifactId>sonar-java-api</artifactId>
			<scope>provided</scope>
		</dependency>

        <!-- unit tests -->
        <dependency>
            <groupId>org.codehaus.sonar</groupId>
            <artifactId>sonar-testing-harness</artifactId>
        	<scope>test</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
        </dependency>
    </dependencies>


</project>
