<jqa:jqassistant-rules xmlns:jqa="http://www.buschmais.com/jqassistant/core/analysis/rules/schema/v1.0">

    <concept id="java:AssignableFrom">
        <description>Creates a relationship ASSIGNABLE_FROM between CLASS labeled nodes if two classes are assignable.</description>
        <cypher><![CDATA[
            MATCH (c:CLASS)-[:IMPLEMENTS|INHERITS*]->(s)
            CREATE UNIQUE
                (c)-[:ASSIGNABLE_FROM]->(c),
                (s)-[:ASSIGNABLE_FROM]->(c)
            RETURN count(c) AS assignableClasses
        ]]></cypher>
    </concept>

</jqa:jqassistant-rules>