#  Valid example configuration of Dropwizard

dataDirectory: /Users/user/code/system/examples

server:
 maxThreads: 1024

 applicationConnectors:
   - type: http
     port: 8080

 adminConnectors:
   - type: http
     port: 8081

 requestLog:
   appenders:
     - type: console
       threshold: OFF

